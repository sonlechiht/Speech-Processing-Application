<UserControl x:Class="SpeechProcessing.Views.UserControl_SplashScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpeechProcessing.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Storyboard x:Key="mystoryboard" Name="splashscreen">
            <DoubleAnimation Storyboard.TargetName="stackPanel" BeginTime="0:0:0.5" Duration="0:0:2" Storyboard.TargetProperty="Opacity" From="1" To="0"/>
            <ObjectAnimationUsingKeyFrames BeginTime="0:0:0.5" Storyboard.TargetProperty="Visibility" Storyboard.TargetName="stackPanel">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}" />
                <DiscreteObjectKeyFrame KeyTime="0:0:2" Value="{x:Static Visibility.Collapsed}" />
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>
    <Border CornerRadius="12">
        <Border.Background>
            <ImageBrush ImageSource="/Resources/background.png"/>
        </Border.Background>
        <StackPanel VerticalAlignment="Center" x:Name="stackPanel" IsVisibleChanged="stackPanel_IsVisibleChanged">
            <Label Content="DSS Speech Processing" FontSize="60" HorizontalAlignment="Center"/>
            <Label Content="Version 1.1.0" FontSize="45" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
    
</UserControl>
